# Apex 灵敏度实验室 — 项目概览

## 项目概述
面向 Apex Legends 玩家的纯前端单文件网页工具，通过标准化五维瞄准测试，帮助玩家快速自测并找到适配自身操作习惯的鼠标灵敏度。

## 交付物
- `index.html` — 单文件 HTML 应用（1893 行），所有代码内联，可离线运行
- 本地预览: http://127.0.0.1:8090

## 核心功能模块

### 1. 五维灵敏度自测（Canvas + Pointer Lock API）
| 测试项目 | 测试规则 | 评分维度 |
|----------|----------|----------|
| 转身校准 | 模拟游戏灵敏度完成 360° 旋转 | 完成速度 + 旋转精度 |
| 跟枪测试 | 追踪移动靶标（匀速/变速/不规则 3 种模式） | 靶心停留占比（跟枪率 %） |
| 急停定位 | 随机位置 10 个靶标依次点击 | 平均反应时间 + 命中偏差 |
| 多目标转移 | 按序命中 3 个靶标 × 5 轮 | 转移速度 + 命中率 |
| 压枪测试 | 补偿后坐力保持准星在目标区域 | 垂直方向平均偏差 |

**前置配置**：DPI、游戏灵敏度、操作流派（手腕流/手臂流）均为必填，未完成时禁止测试并高亮提示。

**测试流程**：图文说明 → 3 秒倒计时 → 正式测试 → 即时得分 → 进入下一项

**异常处理**：Pointer Lock 失败自动暂停弹重试提示；不支持 Pointer Lock 的浏览器禁用自测并提示。

### 2. 智能参数推荐系统
- 根据控制力/速度得分判断灵敏度偏高/偏低/均衡，计算最优基准灵敏度
- 三套方案：保守稳定型(基准×0.85)、均衡通用型(基准)、激进跟枪型(基准×1.15)
- 根据用户选择的操作流派标记推荐方案
- 附带文字解读（如"当前灵敏度偏高，定位反应快但跟枪稳定性不足，推荐优先尝试均衡方案"）
- 自动计算 eDPI、cm/360（保留 2 位小数）
- FOV 手感统一计算器（0% Monitor Distance 算法）
- 灵敏度过高/过低时补充风险提示

### 3. 全套游戏参数优化
- 5 个维度：鼠标设置、画面画质、准星设置（含实时预览）、音效设置、按键布局
- 排位模式 / 练枪模式 双配置切换
- JSON 配置导入/导出，格式错误时友好提示不覆盖原数据

### 4. 实用工具集
- 跨游戏灵敏度转换器（8 款 FPS 游戏互转）
- 职业选手参数库（10 位 ALGS 选手，支持一键套用）
- 瞄准练习方案（5min 热身 / 20min 日常 / 10min 赛前）

### 5. 本地数据持久化
- localStorage 存储所有数据
- 一键清空所有本地数据
- 容量不足时提示用户清理历史

## 技术规范
- **技术栈**: HTML + CSS + Vanilla JavaScript + Tailwind CSS (CDN) + Chart.js (CDN)
- **视觉风格**: 深色电竞简约风，科技蓝(#3b82f6)/暗紫色系(#8b5cf6)，低饱和度
- **性能**: Canvas 使用 requestAnimationFrame 渲染，稳定 60fps
- **精度**: 灵敏度、cm/360 保留 2 位小数
- **兼容性**: Chrome 90+/Edge 90+ 优先，Firefox 88+ 兼容
- **代码行数**: 1893 行（限制 2000 行以内）

## 第二次迭代调整记录
1. 视觉风格从红橙 Apex 主题改为科技蓝/暗紫色系
2. 前置配置增加操作流派选择（必填）
3. 所有数值输入增加合法性校验，非法时标红提示
4. 倒计时改为标准 3 秒，带动画
5. Pointer Lock 异常处理完善（失败重试 + 浏览器兼容检测）
6. 推荐引擎重构为基准灵敏度 + ±15% 三套方案
7. 推荐结果增加文字解读和风险提示
8. cm/360 精度统一为 2 位小数
9. 准星设置增加实时预览画布
10. 增加一键清空数据功能
11. localStorage 容量不足提示
12. 练习方案时长修正
